# Backend API (no trailing slash). Local dev: http://localhost:4000
# Vercel production: set to your deployed API origin, e.g. https://your-api.railway.app
# Must match the API that sets the session cookie; use credentials (withCredentials) in the app.
NEXT_PUBLIC_API_URL=http://localhost:4000
